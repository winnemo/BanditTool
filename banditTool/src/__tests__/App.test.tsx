import { describe, it, expect } from 'vitest';

describe('App', () => {
    it('should render correctly', () => {
        // Dieser Test ist ein Platzhalter und prüft nur, dass 'true' wahr ist.
        // später mit echten Test ersetzen.
        expect(true).toBe(true);
    });
});